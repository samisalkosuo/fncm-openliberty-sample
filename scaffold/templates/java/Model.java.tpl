package dev.fncm.model;

/**
 * Typed result for __NAME__.
 * Serialised to JSON automatically by Liberty JSON-B.
 *
 * Add or remove record components to match the data returned by __NAME__Operation.
 */
public record __NAME__Result(
        String status,
        String message) {}

// Made with Bob
